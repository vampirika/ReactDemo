import React, {  } from 'react';
/*
const DIFFICULTY = {
    SLOW: { label: 'Slow', value: 200 },
    MEDIUM: { label: 'Medium', value: 100 },
    FAST: { label: 'Fast', value: 50 }
  };
*/
const GameNumbers = () => {
  //const canvasRef = useRef<HTMLCanvasElement>(null);

  return (
    <div>
      <div>Numbers game test</div>
    </div>
  );
};

export default GameNumbers;